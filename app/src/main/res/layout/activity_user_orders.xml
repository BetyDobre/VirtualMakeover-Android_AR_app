<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".userActivities.UserOrdersActivity">

    <RelativeLayout
        android:id="@+id/rll5"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:layout_alignParentTop="true">

        <TextView
            android:id="@+id/total_price"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Your orders"
            android:textColor="@color/white"
            android:textSize="25dp"
            android:textAlignment="center"
            android:gravity="center"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="10dp"
            />
    </RelativeLayout>

    <TextView
        android:id="@+id/current_order"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Current order:"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="5dp"
        android:textStyle="bold"
        android:textSize="24dp"
        android:fontFamily="casual"
        android:textColor="@color/black"
        android:layout_below="@id/rll5"
        android:visibility="visible"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/user_order_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/current_order">
    </androidx.recyclerview.widget.RecyclerView>

    <TextView
        android:id="@+id/order_history"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="5dp"
        android:text="Orders history:"
        android:textStyle="bold"
        android:textSize="24dp"
        android:fontFamily="casual"
        android:textColor="@color/black"
        android:layout_below="@id/user_order_list"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/user_order_history_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/order_history">

    </androidx.recyclerview.widget.RecyclerView>
</RelativeLayout>